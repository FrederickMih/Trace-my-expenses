<%= form_with(model: outlay) do |form| %>
  <% if outlay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outlay.errors.count, "error") %> prohibited this outlay from being saved:</h2>

      <ul>
        <% outlay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.text_field :author_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
